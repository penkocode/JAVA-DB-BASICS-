SELECT deposit_group,sum(deposit_amount) AS 'total_sum' FROM wizzard_deposits WHERE magic_wand_creator = 'Ollivander family' GROUP BY deposit_group HAVING sum(deposit_amount)< 150000 ORDER BY `total_sum` DESC;